/**
 * Relative-to-origin location paths, i.e. they should all begin with a forward slash as in "/login".
 */
export enum LOCATION_PATHS {
    home = "/",
    itmes = "/items",
    login = "/login",
    signUp = "/sign-up",
    resetPassword = "/reset-password"
}