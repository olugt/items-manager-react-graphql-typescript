/**
 * Helps in type detection and narrowing. Should typically be assigned to a property named "kind" in the type or class. But in the case of classes, the classes must be reconstructed if not previously constructed from the source of the data.
 */
export enum TYPE_KINDS {
    errorDataModel
}